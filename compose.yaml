services:
  ros2-dev:
    build:
      context: .
      dockerfile: Dockerfile
    image: ros2-camera-framework:jazzy
    init: true
    stdin_open: true
    tty: true
    working_dir: /workspace
    volumes:
      - .:/workspace
